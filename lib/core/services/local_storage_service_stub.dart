import 'local_storage_service.dart';

LocalStorageService getLocalStorageService() => throw UnsupportedError(
      'Cannot create LocalStorageService without platform-specific implementation.',
    );
